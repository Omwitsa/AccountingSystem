@page
@model AccountingSystem.Pages.Vendors.ListbillModel
@{
    var count = 0;
    
}
<div>
    <div class="row color m-1" style="color:green">Bills</div>
    <div class="modal-header" style="background-color:white;color:white;text-align:center">
        <div class="row">
            <a class="btn btn-info" asp-page="./Editbills">CREATE</a>
            <a class="btn btn-info" asp-page="./EditRefunds">UPLOAD</a>
            
        </div>
    </div>
    <div class="table-responsive">
        @if (!Model.Success)
        {
            <div class="alert-danger text-center">
                @Model.Message
            </div>
        }
        <table class="table table-sm table-hover">
            <thead>
                <tr>
                    <th scope="col">Number</th>
                    <th scope="col">Vendor</th>
                    <th scope="col">Bill Date</th>
                    <th scope="col">Due Date</th>
                    <th scope="col">Reference</th>
                    <th scope="col">Next Activity</th>
                    <th scope="col">Tax Excluded</th>
                    <th scope="col">Total</th>
                    <th scope="col">Status</th>
                </tr>
            </thead>
            <tbody>
                @*@foreach (var vender in Model.Venders)
                    {
                        count++;
                        <tr>
                            <th scope="row">@count</th>
                            <td>@vender.Name</td>
                            <td>@vender.City</td>
                            <td>@vender.Country</td>
                            <td>@vender.Email</td>
                            <td>@vender.PhoneNo</td>
                            <td>@vender.Industry</td>
                            <td>@vender.PhoneNo</td>
                            <td>@vender.Industry</td>
                        </tr>
                    }*@
            </tbody>
        </table>
    </div>
</div>